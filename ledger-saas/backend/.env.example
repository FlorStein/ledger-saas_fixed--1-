DATABASE_URL=sqlite:///./app.db

JWT_SECRET=change_me
JWT_ALG=HS256
ACCESS_TOKEN_MINUTES=120

UPLOAD_DIR=./uploads

# Frontend origin(s). Include both to avoid CORS errors when switching between
# http://localhost and http://127.0.0.1.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Demo seed controls
SEED_DEMO=true
# Seed example sales (leave false for realistic matching tests)
SEED_SALES=false

# Matching System Configuration
AUTO_MATCH_THRESHOLD=85
AUTO_MATCH_GAP=10
DATE_WINDOW_HOURS=72

# Optional: WhatsApp via Twilio Sandbox (official)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Optional: WhatsApp via Meta Cloud API (official sandbox/test number)
META_WA_ACCESS_TOKEN=
META_WA_PHONE_NUMBER_ID=
META_WA_VERIFY_TOKEN=change_me
